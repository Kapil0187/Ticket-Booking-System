<nav class="navbar navbar-expand-lg navbar-light bg-light">
     <%= link_to "Ticket Booking" , root_path %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="d-flex justify-content-between collapse navbar-collapse" id="navbarSupportedContent">
    <div class='d-flex w-50 justify-content-around'>
        <div>
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <%= link_to "Bus", bus_path %>
            </li>
          </ul>
        </div>
         <div>
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <%= link_to "Train", trains_index_path %>
            </li>
          </ul>
        </div>
        <div>
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <%= link_to "Flight", flights_index_path %>
            </li>
          </ul>
        </div>
        <% if user_signed_in? %>
          <div>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <%= link_to "Your Bookings", allbookings_path(user_id:current_user.id) %>
              </li>
            </ul>
          </div>
        <% end %>
    </div>
    <div class="d-flex flex-row mx-4">
      <% if user_signed_in? %> 
        <div class='mx-4'> Welcome <%= current_user.email %> </div> 
        <%= button_to "Sign out", destroy_user_session_path, method: :delete %> 
      <% else %>
        <%= button_to "Sign in", new_user_session_path %> 
      <% end %>
    </div>
    </form>
  </div>
</nav>

