<% if user_signed_in? %>
  <div>
  </div>
  <%= render :partial => 'form', lacals: {booking: @booking} %>
<% else %>
  <p> Please Sign Up or Sign In to book ticket. </p>
  <h2> <%= link_to "Sign up", new_user_registration_path %>
<% end %>

